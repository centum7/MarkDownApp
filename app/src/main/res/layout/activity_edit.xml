<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:fab="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <include
        android:id="@+id/tool_bar"
        layout="@layout/tool_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/myMemoTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Title"
        android:inputType="textNoSuggestions"
        android:theme="@style/EditTextColorControl" />

    <EditText
        android:id="@+id/myMemoBody"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="top"
        android:hint="Body"
        android:inputType="textMultiLine|textNoSuggestions"
        android:theme="@style/EditTextColorControl" />
    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" >

        <com.getbase.floatingactionbutton.FloatingActionButton
            android:id="@+id/store_button_in_create_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|bottom"
            fab:fab_colorNormal="@color/main_color"
            fab:fab_colorPressed="@color/sub_color"
            fab:fab_icon="@drawable/ic_action_editor_mode_edit"
            fab:fab_title="保存" />

        <com.getbase.floatingactionbutton.FloatingActionsMenu
            android:id="@+id/multiple_actions"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            fab:fab_addButtonColorNormal="@color/white"
            fab:fab_addButtonColorPressed="@color/white_pressed"
            fab:fab_addButtonPlusIconColor="@color/black_semi_transparent"
            fab:fab_labelStyle="@style/menu_labels_style"            >

            <com.getbase.floatingactionbutton.FloatingActionButton
                android:id="@+id/alert_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                fab:fab_colorNormal="@color/main_color"
                fab:fab_colorPressed="@color/sub_color"
                fab:fab_icon="@drawable/ic_action_action_settings"
                fab:fab_size="mini"
                fab:fab_title="設定" />

            <com.getbase.floatingactionbutton.FloatingActionButton
                android:id="@+id/store_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                fab:fab_colorNormal="@color/main_color"
                fab:fab_colorPressed="@color/sub_color"
                fab:fab_icon="@drawable/ic_action_editor_mode_edit"
                fab:fab_size="mini"
                fab:fab_title="保存" />
        </com.getbase.floatingactionbutton.FloatingActionsMenu>

    </FrameLayout>
</LinearLayout>